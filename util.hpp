#pragma once

enum Color {
    RED,
    YELLOW,
    BLUE,
    GREEN
};

enum Symbol {
    CIRCLE,
    SQUARE,
    TRIANGLE,
    DIAMOND,
    MULTICOLOR
};

enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
};

enum Angle {
    HD,
    BD,
    BG,
    HG,
    NONE
};